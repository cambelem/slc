{START_FORM}
<div class="row">
    <div class="col-md-4">
        <div class="form-group">
            <label for"{BANNER_ID_ID}">{BANNER_ID_LABEL_TEXT}</label>
            <input type="text" class="form-control" id="{BANNER_ID_ID}" name="{BANNER_ID_NAME}" placeholder="Enter Banner ID">
        </div>

    </div>
    <div class="col-md-2">
        <div class="form-group">
            <button type="submit" class="btn btn-primary btn-md" style="margin-top: 1.7em;">{SUBMIT_VALUE}</button>
        </div>
    </div>
</div>
{END_FORM}
